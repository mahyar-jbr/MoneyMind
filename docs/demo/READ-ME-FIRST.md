# Hey Kasra — read this first

This folder has everything for the demo video. Two files, three companions:

1. **`SCRIPT.md`** — what to record + what to say + what shows on screen, in order, beat by beat. Read this once start to finish. ~10 min read.
2. **`SUBTITLES.srt`** — drop this into the editing timeline at the end.
3. **`companions/`** — three HTML pages we screen-record for the parts where the app alone isn't enough (architecture, 18 tools, memory loop). Open them, hit `F11`, hit `R` to play.

## What I'd love you to do, in order

1. Read `SCRIPT.md` end-to-end. 10 min.
2. Open `companions/architecture.html` and `companions/tools.html` in Chrome to see how they animate. They auto-play when the page loads, press **R** to restart.
3. Tell me if anything looks wrong or you want changed.
4. When you're ready to record: follow `SCRIPT.md` — it has the recording setup, the prompts to type, the exact moments to click.
5. Hand the raw clips + the `companions/*.html` files to the editor. They drop `SUBTITLES.srt` in the timeline.

## What's different from the script you sent

Your version was solid. I changed three things to make it punchier:

1. **The opening hook is now "drag a real bank statement PDF into the chat and watch the agent parse it in real-time."** That's the most visual moment in the app, the one judges will remember. Yours started with text transaction cards — less cinematic.
2. **The intervention beat is now `cap food spending` instead of `meal-prep reminder`.** Caps actually write a budget into the database when accepted (V6 we just shipped) — so we can SHOW the dashboard updating live, which proves persistence way better than refreshing a page.
3. **The architecture beat is one of our HTML pages**, not a static graphic. It animates the request flow with arrows + lights up all 18 tools. Much stronger story for a hackathon judge than a still image.

Length-wise we're at **2:20** which leaves you headroom under 2:30.

## Stuff I need from you

- **A voice** (you, or AI voice with our consent, or someone else). Reading the lines in `SCRIPT.md` voiceover column.
- **Music license.** Quiet ambient bed under the voice. Your call on the track.
- **The cut.** Final Cut or DaVinci, you've done more of this than me.

Everything else is in here. Ping me if anything's unclear.
